# Настройка MongoDB для Russian Trader

## 🇷🇺 ВАЖНО ДЛЯ РОССИЙСКИХ ПОЛЬЗОВАТЕЛЕЙ

**MongoDB Atlas недоступен в России** из-за санкционных ограничений. Используйте альтернативные варианты:

## 🚀 Быстрая настройка

### Вариант 1: Локальная установка MongoDB (РЕКОМЕНДУЕТСЯ для России)

#### Windows
1. **Скачайте MongoDB Community Server** с [официального сайта](https://www.mongodb.com/try/download/community)
2. **Установите** с настройками по умолчанию
3. **Запустите MongoDB как службу:**
   ```bash
   # Проверьте, что служба запущена
   net start MongoDB
   ```
4. **Или используйте MongoDB Compass** для графического интерфейса

#### macOS
```bash
# Установка через Homebrew
brew tap mongodb/brew
brew install mongodb-community

# Запуск
brew services start mongodb-community
```

#### Linux (Ubuntu/Debian)
```bash
# Импорт ключа
wget -qO - https://www.mongodb.org/static/pgp/server-7.0.asc | sudo apt-key add -

# Добавление репозитория
echo "deb [ arch=amd64,arm64 ] https://repo.mongodb.org/apt/ubuntu focal/mongodb-org/7.0 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-7.0.list

# Установка
sudo apt-get update
sudo apt-get install -y mongodb-org

# Запуск
sudo systemctl start mongod
sudo systemctl enable mongod
```

### Вариант 2: Альтернативные облачные базы (для пользователей за пределами России)

#### MongoDB Atlas (если доступен в вашем регионе)
1. **Создайте аккаунт на [MongoDB Atlas](https://www.mongodb.com/atlas)**
2. **Создайте кластер** (бесплатный tier M0 доступен)
3. **Настройте доступ:**
   - Добавьте IP адрес `0.0.0.0/0` для доступа отовсюду
   - Создайте пользователя базы данных
4. **Получите строку подключения:**
   ```
   mongodb+srv://username:password@cluster.mongodb.net/russian-trader
   ```
5. **Добавьте в `.env` файл:**
   ```env
   MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/russian-trader
   ```

### Вариант 3: Российские альтернативы

#### Yandex Cloud Managed MongoDB
- **Доступно в России** через Yandex Cloud
- **Инструкции:** https://cloud.yandex.ru/services/managed-mongodb
- **Стоимость:** от 500 руб/месяц

#### Selectel MongoDB
- **Российский провайдер** с MongoDB как сервис
- **Инструкции:** https://selectel.ru/services/cloud/databases/mongodb/
- **Стоимость:** от 300 руб/месяц

#### Timeweb Cloud
- **Российский хостинг** с поддержкой MongoDB
- **Инструкции:** https://timeweb.cloud/services/databases
- **Стоимость:** от 200 руб/месяц

### Вариант 2: Локальная установка MongoDB

#### Windows
1. **Скачайте MongoDB Community Server** с [официального сайта](https://www.mongodb.com/try/download/community)
2. **Установите** с настройками по умолчанию
3. **Запустите MongoDB как службу:**
   ```bash
   # Проверьте, что служба запущена
   net start MongoDB
   ```
4. **Или используйте MongoDB Compass** для графического интерфейса

#### macOS
```bash
# Установка через Homebrew
brew tap mongodb/brew
brew install mongodb-community

# Запуск
brew services start mongodb-community
```

#### Linux (Ubuntu/Debian)
```bash
# Импорт ключа
wget -qO - https://www.mongodb.org/static/pgp/server-7.0.asc | sudo apt-key add -

# Добавление репозитория
echo "deb [ arch=amd64,arm64 ] https://repo.mongodb.org/apt/ubuntu focal/mongodb-org/7.0 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-7.0.list

# Установка
sudo apt-get update
sudo apt-get install -y mongodb-org

# Запуск
sudo systemctl start mongod
sudo systemctl enable mongod
```

### Вариант 3: Docker (Альтернатива)

```bash
# Запуск MongoDB в Docker
docker run -d -p 27017:27017 --name mongodb mongo:latest

# Или используйте docker-compose (файл уже создан в проекте)
docker-compose up -d
```

## ⚙️ Настройка окружения

### Файл `.env` в папке `backend/`

```env
# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/russian-trader
# ИЛИ для MongoDB Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/russian-trader

# Fallback на временное хранилище (если MongoDB недоступна)
ENABLE_MOCK_DATA=true
```

## 🔧 Проверка подключения

### Тестирование подключения
```bash
cd backend
npm run dev
```

Если подключение успешно, вы увидите:
```
✅ MongoDB успешно подключена
```

### Если MongoDB недоступна
Приложение автоматически переключится на временное хранилище в памяти:
```
⚠️ MongoDB недоступна, используется временное хранилище
```

## 🛠️ Устранение проблем

### Ошибка: "ECONNREFUSED"
- **Проверьте, запущен ли MongoDB:** `mongod --version`
- **Проверьте порт 27017:** `netstat -an | findstr 27017` (Windows)
- **Перезапустите MongoDB службу**

### Ошибка аутентификации в Atlas
- **Проверьте username/password** в строке подключения
- **Убедитесь, что IP адрес добавлен** в Network Access
- **Проверьте имя кластера** в строке подключения

### Ошибка: "Duplicate schema index"
- **Удалите дублирующиеся индексы** в моделях MongoDB
- **Или пересоздайте коллекцию**

## 📊 Миграция данных

### Из временного хранилища в MongoDB
При первом запуске с подключенной MongoDB:
- Демо-пользователь будет создан автоматически
- Все новые пользователи сохраняются в базе
- Данные сохраняются между перезапусками сервера

### Резервное копирование
```bash
# Экспорт данных
mongodump --uri="mongodb://localhost:27017/russian-trader"

# Импорт данных
mongorestore --uri="mongodb://localhost:27017/russian-trader"
```

## 🔒 Безопасность

### Рекомендации для продакшена
- Используйте **MongoDB Atlas** для продакшена
- Настройте **VPC Peering** для безопасности
- Используйте **SSL/TLS** соединения
- Регулярно делайте **бэкапы**
- Настройте **мониторинг** и алерты

### Локальная разработка
- Для разработки можно использовать временное хранилище
- Данные сбрасываются при перезапуске сервера
- Идеально для тестирования и демо

## 🎯 Быстрый старт для разработки

Если вы хотите быстро начать разработку без установки MongoDB:

1. **Убедитесь, что в `.env` установлено:**
   ```env
   ENABLE_MOCK_DATA=true
   ```

2. **Запустите сервер:**
   ```bash
   cd backend && npm run dev
   ```

3. **Приложение будет использовать временное хранилище** с полной функциональностью

Приложение автоматически определит доступность MongoDB и выберет оптимальный режим работы!